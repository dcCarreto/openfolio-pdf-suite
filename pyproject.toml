[project]
name = "openfolio-pdf-suite"
version = "0.1.0"
description = "Suite completa e open source de manipulação de PDF, 100% local, sem paywall."
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
